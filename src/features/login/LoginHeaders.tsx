import styles from "./styles/loginPage.module.css";

export const LoginHeaders = () => {
  return (
    <div className={styles.loginHeaders}>
      <span className={styles.loginBoldText}>Login</span>
    </div>
  );
};
